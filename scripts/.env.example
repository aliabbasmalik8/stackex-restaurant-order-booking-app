# Postgres — shared by all scripts under scripts/
DATABASE_URL=postgres://postgres:postgres@localhost:5432/order-booking

# Optional: override seed file (default: ./seed-data.json)
# SEED_DATA_PATH=

# Optional: create:admin overrides
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=PreviewAdmin123!
# ADMIN_DISPLAY_NAME=Preview Admin
