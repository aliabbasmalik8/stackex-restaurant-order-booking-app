# Feature: `createAccountPassword`

Default: **enabled**. Email/password sign-up.

- Registry: `createAccountPassword`
- UI: create-account password form — `isServiceInteractive('createAccountPassword')`
- API: `POST /api/users/signup`
