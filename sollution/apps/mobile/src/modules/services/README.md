# Services (feature registry)

→ **[`ai_instruction/features/`](../../../ai_instruction/features/README.md)**

Injectable, env-gated capabilities. Use `isServiceInteractive` / `shouldRenderService` — never raw `EXPO_PUBLIC_SERVICE_*` in screens.
